{
  "scope-boundary": "Change the opt-in Pi replacement candidate so the real @earendil-works/pi-agent-core runtime owns production agentic turn progression and tool-loop execution across the scoped Istara seams, while Istara continues to own authentication, project authorization, canonical tool implementations, persisted product state, research validity, human Done/report approval, telemetry, and rollback. Replace the current model-name-only DeepSeek shim with endpoint-identity routing that supports configured API, OpenAI-compatible, and Anthropic-compatible endpoints and cannot select relay/browser donated compute even when a donor advertises the same model alias. Preserve ordinary Petals-style compute donation, relay/browser scheduling, connection strings, project scoping, and security behavior unchanged for non-Pi routes. Include production-path chat, A2A, local in-process channel, governed Autoresearch, source/evidence/Done gates, memory/RAG/ReasoningBank/Memento/skill stats, steering, telemetry, all 15 experiment scenarios, and predefined regression/security/docs evidence. Keep Pi default-off and reversible. Do not run live external channel traffic, do not load local models, do not modify protected LLMs/ or Model_Finetuning/, and do not push the local Review_pi_test branch. Bounded DeepSeek API verification may use the existing Keychain secret under the original cumulative USD 0.50 experiment cap with redacted raw evidence."
}
