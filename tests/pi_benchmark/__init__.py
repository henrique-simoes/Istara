"""Pi-vs-Legacy benchmark apparatus (master plan §10).

Task B0-1 delivers the schema-first foundation: :mod:`schema` loads and validates
``comparison-Istara-pi/metrics-schema.json``, the contract every downstream asset
(runner, feature-criteria compiler, JudgeLayer, report generator) conforms to.
"""
