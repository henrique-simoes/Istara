{
  "scope-boundary": "CHANGES: (1) F-12 fix is DONE separately (transport surfaces reasoning-overflow; committed). (2) UI: in the general settings area where API configuration currently lives, add an agentic-core selector with exactly two options — 'pi' and 'istara' (the legacy engine is named 'istara' in UI copy) — persisted as the global default engine (backend setting, not just projects.agentic_engine); follows Istara's existing visual style/components, no deviation. (3) Pi model management in Istara UI: the PiModelManager catalog (API endpoints + cloud models: endpoint_id, provider_kind, base_url, model, keychain service, timeout/retries, pricing, context window, supports_tools/vision) becomes manageable from Istara's backend API + frontend settings UI (list/add/edit/delete pi_api_endpoints entries persisted in settings), with the same look as the current LLM-server management UI. (4) Retirement: for API endpoints and cloud models, PiModelManager is THE management plane — legacy LLM-server management for cloud/API is marked legacy/retired in UI copy (rows still project read-only into the Pi catalog, petals/donated compute stays untouched in ComputeRegistry and keeps working with BOTH agentic cores). (5) pi packages bumped to 0.83.0 (DONE, committed). MUST STAY UNCHANGED: petals bridge semantics + consent model, the engine-resolution order (per-call > header > project > global default), pi_runtime isolation invariant, visual style of the settings area, all CF-SPEC-10/11 artifacts. Tests: backend API tests for the new settings endpoints, frontend component tests for the selector + pi endpoint manager, pi_production + benchmark suites green, security benchmark + feature docs regenerated."
}
