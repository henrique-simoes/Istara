{
  "scope-boundary": "Repair only defects confirmed by the 76-scenario Mac Studio QA sweep: keep prompt composition within its requested token budget while retaining identity and the Research Spine notice; make project export use a safe writable fallback in non-root Docker; make governed meta-hyperagent toggle report in-memory state when .env persistence is unavailable without claiming persistence; synchronize the simulation API client's authenticated token; and align stale simulation assertions with the current retired-route, redacted-catalog, backup-component, cross-container-folder, degraded-health, and current project-settings contracts.",
  "contract-compatibility": "Do not resurrect retired /api/llm-servers routes, weaken watch-folder path validation, expose credentials, or alter live provider/model behavior. Preserve authorization, project scope, fail-closed provider behavior, and truthful persistence/readiness reporting. Scenario-only changes must assert the current contract or explicitly classify environment-limited checks.",
  "verification-bar": "Use TDD for prompt budget, Docker export fallback, and meta-hyperagent persistence behavior. Run focused backend tests, simulation static checks, targeted Mac Studio Docker scenarios for every changed contract, the full 76-scenario sweep, frontend build/tests, security benchmark for self-evolution/auth-sensitive surfaces, feature-doc generation/check, Compass Forge gate-before/after, and attach command evidence.",
  "environment-boundary": "Run live Istara Docker only on Mac Studio using the disposable istara-qa-testing-20260829 stack. Recreate only QA services when required after inventory/backups. Never touch Plex, istara-r9-final, non-QA containers, external providers, live models, or distributed compute/donor surfaces.",
  "rollback": "Revert only the bounded source/test/scenario changes and remove this clarification file; retain unrelated ambient worktree edits and QA backups.",
  "docs": "Run python scripts/feature_docs.py --seed-missing --generate-site --check after route/store/agent/model/test contract changes, and append identical evidence to both Build Stream ledgers before and after each material change."
}
