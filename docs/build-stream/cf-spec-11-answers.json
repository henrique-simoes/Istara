{
  "scope-boundary": "CHANGES: (1) tests/pi_benchmark features+probes packs: execute live waves for both engines and score criteria deterministically (axes 2, 9). (2) Memory-load wave via runner --memory-load (axis 5). (3) scripts/run_istara_evals.py gains --engine {legacy,pi}: live cases route through AgenticDispatcher.completion (engine-selected) instead of direct compute_registry.chat, so Istara's own eval suites (classic_llm, dag_react, rag, prompt_rag, llmlingua, memento_skills, meta_hyperagent, thinking_output, orchestration_benchmarks) run identically through BOTH engines — deep corpus for axes 4, 7, 8, 9, 10 and axis-3 deterministic checks; default behavior without --engine stays byte-identical (legacy, backward compatible). (4) Per-axis metrics + rationale layer: every axis gets measured values AND a written rationale (what it measures, why, threats) feeding comparison-Istara-pi/article/ for the future arXiv paper; methodology follows the KDD'25 agent-evaluation taxonomy (behavior/capabilities/reliability/safety) and process-level evaluation practice. (5) Judging pass 2 over deep-corpus outputs (blind A/B where judge-based, deterministic where ground truth exists). (6) Final scorecard/report with all 10 axes judged or explicitly blocked-with-reason. MUST STAY UNCHANGED: engine parity rules (same DeepSeek provider/model both arms, DEC-7 canon), record/ledger immutability (new manifests/records only), pi_runtime isolation invariant, the accepted CF-SPEC-10 artifacts (untouched), spend within the DEC-8 $3.00 envelope (est. $1.2-1.8 total incremental). Spend estimate gate before any live wave."
}
