{
  "scope-boundary": "Changes (all inside the benchmark apparatus + report layer, in the retake worktree on branch conductor/pi-bench-retake-20260722): (1) F-11 fix — benchmark legacy-arm units must dispatch through AgenticDispatcher.ensemble with engine=legacy routed to a benchmark-seeded registry endpoint (approved DeepSeek only), restoring the production legacy ReAct loop as the DUT; ordinary production legacy routing stays ComputeRegistry-backed and unchanged. (2) Raw capture — the live driver writes prompts.jsonl.gz / outputs.jsonl.gz per comparison-Istara-pi/raw-llm-output-capture-requirements.md (call_id, engine_path, settings, messages, tool schemas, outputs, stop reason, latency, usage, capping) with the documented redaction policy; no secrets ever captured. (3) New industry scenario pack — BFCL v4 subset (tool calling), tau-bench subset (multi-turn tool-agent), GAIA subset (general assistant) executed through the same paired runner for both engines. (4) Re-dispatch of none + self_moa lane units for BOTH engines with capture enabled. (5) Kimi blind judging session per plan C section 2.6 contract (artifact-only, read-only DUT ledger, additive outputs under reports/<ts>-judging/). (6) Report regeneration with judged quality axes. MUST STAY UNCHANGED: pi_runtime isolation invariant (never imports/mutates ComputeRegistry), donated-compute/petals paths, production feature behavior, the closed historical records/ledger rows (new records only, never rewrites), the count-to-zero migration ratchet. Budget: owner approved raising the cumulative spend cap; proposed new envelope $3.00 total (≈$0.41 already spent + ≈$2.3 estimated for re-dispatch, industry pack, and retries), enforced by the same reserve-before-dispatch ledger; final estimate gate at B0-style scheduling before any spend."
}
