{
  "scope-boundary": "CHANGES: fix F3, F5, F7-F17 toward docs/architecture/research-validity-contract.md and docs/architecture/self-improvement-governance-contract.md (untrusted-wrapper truncation and delimiter injection; document tags cannot pin or escape; LLM-derived artifacts and agent notes leave the source evidence index and cannot confirm claims; exact agent-note scoping; plan-DAG steps never share one AsyncSession concurrently and dependents of a failed step do not run; autoresearch rag_params optimises qrels nDCG@10 on a sandbox index re-built per chunking setting and never mutates process-global settings; self-evolution promotions and caches are project-scoped or invalidated; learned skill-routing boosts cannot lift an irrelevant skill over the floor; same-dimension embedder swaps are detected by a probe fingerprint; the model and tools see ranks, not RRF fused values; findings search never scores provisional findings above source evidence by construction; compression keeps retrieval order; the file watcher keeps BM25 rows; no ciphertext is indexed; reads have no side effects; ReasoningBank retrieval is not capped to the 200 newest rows; zero is not treated as unset; top_k is bounded; memory listing does not load whole tables; dead code removed) and add six measurements as reusable harnesses (qrels with Recall@k/nDCG@10/MRR and bootstrap CIs, re-indexing ablations with paired randomization tests, budget recall, judge-validated faithfulness and context precision on the live lane, provenance coverage as a health invariant, learning-loop safety). MUST STAY UNCHANGED: coding-run reliability rules (>=3 distinct model identities, served_model proof, exact-substring quotes, duplicate ratings fail closed, kappa >= 0.60), report gates, auth/authorization, project isolation, the engine-resolution order, content-free telemetry, the QA provider-stub lane staying credential-free. Verification: a pytest per behaviour change that fails on origin/main; Playwright scenarios driven in the container lane across roles, light/dark, 375px, keyboard focus, loading/error/empty and axe WCAG 2.1 AA; security benchmark; change and feature obligation checkers; CF gate before/after with no new drift."
}
