Root: `<repo-root>-pi-replacement` (branch `Review_pi_test`). All paths below are relative to that root.

# 1. Semantics parity checklist

Tag legend: **[API]** = API-replaceable (meaningful against static OpenAI-compatible endpoints) · **[POOL]** = only meaningful for the dynamic donated/local pool · **[LOCAL]** = only meaningful for local Ollama/LM Studio serving (lost if addressed as plain `openai_compat`).

| # | Behavior | Where (file:line) | Tag |
|---|---|---|---|
| 1 | Model alias resolution: `name` ⇄ `name:latest` alias set; node "supports model" = intersection of loaded_models ∪ capability keys with aliases | `backend/app/core/compute_registry_routing.py:230-246` (`_model_aliases`, `_node_supports_model`) | [API] |
| 2 | Requested-model resolution per node (fallback to node's known chat models, configured `lmstudio_model` authority, vision-preferred pick, embed-model exclusion by `"embed" in name`) | `backend/app/core/compute_node_models.py:192-280` (`_resolve_model`); `:161-190` (`_known_chat_models`); effective requested model from `settings.lmstudio_model` when `strict_auto_routing` and provider=lmstudio: `backend/app/core/compute_registry_invocation.py:167-182` | [API] |
| 3 | strict_model_routing: setting `strict_auto_routing` default False `backend/app/config.py:168`; admin toggle `backend/app/api/routes/settings.py:423-440`; chat uses setting `compute_registry_invocation.py:239-241`; chat_stream accepts per-call override param `:557,580-584` (passed `True` for Pi candidate: `backend/app/api/routes/chat.py:323,511`); strict filter drops nodes that can't attempt the model `compute_registry_routing.py:153-159`, `:172-190` (strict path sorts donors first when project-scoped `:181-190`); non-strict only *prefers* model-capable nodes `:191-201`; `_node_can_attempt_requested_model` lets configured LM Studio host attempt an unlisted configured model `:249-264` | [API] (LM Studio carve-out is [LOCAL]) |
| 4 | min_context admission: auto-estimate from messages+max_tokens+response_format+tools (`count_tokens`+4/msg, default 512) `compute_registry_routing.py:797-822`; applied in chat/stream when caller passes 0 `compute_registry_invocation.py:242-248,585-586`; candidate filter keeps only nodes with a model whose `context_length >= min_context` — but **soft**: falls back to all candidates if none qualify `compute_registry_routing.py:161-170` (same soft rule in `_sorted_servers` `:73-82`); context-length error detection markers `compute_registry_helpers.py:227-242`; on context error mid-request: LM Studio context reload else skip node `compute_registry_invocation.py:469-485` (chat), `:850-866` (stream) | [API] (filter/estimate) + [LOCAL] (reload) |
| 5 | Vision capability filtering: message scan for `image`/`image_url`/`input_image` blocks (recursive) `compute_registry_routing.py:276-297`; require_vision drops nodes without any `supports_vision` capability (hard filter, no unknown-caps grace) `:145-151` and `:66-72`; per-model vision check `:266-274`; when vision needed and model wasn't explicitly requested, model pin is cleared `compute_registry_invocation.py:229-230,570-571`; dedicated error "No vision-capable compute nodes available for image chat" `:540-543,918-921` | [API] |
| 6 | Image block translation: **only for Anthropic-family nodes** — OpenAI multimodal blocks → Anthropic `image`/`base64` blocks `backend/app/core/compute_node_transport.py:143-185` (`_anthropic_content`), messages conversion `:187-204`. OpenAI-compatible/Ollama nodes receive list-content verbatim (`_sanitize_messages` passes dict/list content through `compute_registry_routing.py:777-778`) | [API] |
| 7 | response_format/schema adaptation per provider family: canonical OpenAI `json_schema` wrapper `backend/app/core/llm_schema_adapter.py:57-73`; dispatch `provider_response_format_fields` `:111-133` — ollama→`format` `:102-108,120-121`; gemini-native→`generationConfig.responseMimeType/responseSchema` `:122-130`; anthropic→no field, instead forced tool `anthropic_structured_output_tool` `:136-152` wired in `compute_node_transport.py:249-256` and tool_use→JSON text normalization `llm_schema_adapter.py:155-163` + `compute_node_transport.py:266-271`; default→`response_format` passthrough `:133`. Applied at `compute_registry_invocation.py:325-331,387-393` and `compute_node_invocation.py:91-92,135-136` | [API] |
| 8 | Retries/fallback-on-unavailable: transient statuses {408,409,425,429,500,502,503,504} + timeout/network `compute_registry_helpers.py:13`, `compute_registry_routing.py:732-739`; 5 attempts/node, expo backoff 0.25s→2.0s cap `compute_registry_helpers.py:14-16`, `compute_registry_routing.py:741-745`; per-node attempt loop then next candidate `compute_registry_invocation.py:251-544` (chat), `:589-922` (stream — **no retry once a chunk was emitted** `:874-891`); model-availability error markers `compute_registry_helpers.py:208-224` trigger JIT load recovery `compute_registry_invocation.py:439-468,818-849` (max 1 LM Studio load per request `:450-458,831-839`); `include_unhealthy=True` rescue tier (cb-available, not auth/cooldown/no_model_server) `compute_registry_routing.py:111-125`; terminal `RuntimeError("No compute nodes available for chat/streaming")` `compute_registry_invocation.py:544,922` | [API] |
| 9 | Circuit breakers: 3-state closed/open/half_open, threshold 5, 60s cooldown, slow >10s marks state only `backend/app/core/compute_node_transport.py:62-103` (fields `compute_node.py:37-44`); gates candidate selection `compute_registry_routing.py:104,118`; success/failure recording incl. 3-consecutive-failure node cooldown (60s, `health_state="cooldown"`) `compute_registry_routing.py:656-718`; auxiliary (embed) failures only degrade, never take chat node offline `:719-730` | [API] |
| 10 | Health/latency probing: per-node `check_health` probes `/api/tags` (ollama) or `v1/models`, records `latency_ms`, health_state taxonomy (`ready/no_model_loaded/auth_required/unreachable/timeout/unhealthy`), auto-corrects mislabeled ollama→openai_compat `backend/app/core/compute_node_models.py:24-133`; registry sweep + passive capability detection + context sync `compute_registry_lifecycle.py:435-508`; `ensure_chat_ready` real-chat readiness (1-token LM Studio probe, 300s/10s pos/neg cache) `:514-603`; background loop 60s `:605-612` (started only via facade callers; startup one-shot `backend/app/main.py:472-480`) | [API] (relay heartbeat branch `:443-476` is [POOL]) |
| 11 | Capacity scoring: `node_capacity_score` — hard-zero on unhealthy/auth/no-model/timeout/unreachable, saturation (`active_requests>=max_active_requests`, default 4 `compute_node.py:61`), cooldown; 100 − 15·active − latency/10 (cap 30) − priority + RAM bonus (cap 20) − low-RAM/CPU penalties `backend/app/core/compute_capacity.py:9-44`; sort key (healthy, priority, −score) `compute_registry_routing.py:203`; pool envelope (slots/utilization/saturated) `compute_capacity.py:47-71`; `get_stats` incl. endpoint+hardware dedupe and swarm tier (local_only→full_swarm) `compute_registry_invocation.py:1231-1273`, dedupe helpers `:43-163` | [API] (RAM/CPU inputs + swarm tiers effectively [POOL]) |
| 12 | Context-window detection/sync: detection from LM Studio native `api/v1/models` (trained/loaded ctx, `is_loaded`, `loadable`) `backend/app/core/model_capabilities.py:285-331,446-465`; Ollama `/api/ps`+`/api/show`+`/api/tags` `:367-443`; generic `/v1/models` metadata keys (`max_model_len`, `n_ctx`, …) `:334-364,190-219`; name heuristics fallback `:85-172`; sync to global budget `settings.update_context_window` (only on >2× change) `backend/app/config.py:329-345`, called from `compute_registry_lifecycle.py:499-504` and facade `compute_registry.py:35-50` | [API] (generic metadata) / [LOCAL] (native LM Studio/Ollama depth) |
| 13 | ensure_model/pull/JIT-load: `pull_model` streams Ollama `/api/pull` on first healthy ollama node `compute_registry_invocation.py:1153-1166`; `ensure_model` (list→prefix match→pull) `:1168-1177`; embed model bootstrap `backend/app/core/embeddings.py:100-103`; admin model-switch pull `backend/app/api/routes/settings.py:272-289`; LM Studio JIT load `api/v1/models/load` (+global load lock, context_length echo) `compute_node_models.py:282-363`, unload-before-reload `:365-407`; registry recovery orchestration (`_recover_unloaded_node_model`, `_recover_context_node_model`, `_ensure_node_model_ready`, context target rounding w/ 1.5× headroom min 8192) `compute_registry_routing.py:350-633`; gated by settings `lmstudio_auto_load_enabled/auto_context_reload/max_load_attempts_per_request/allow_unload_on_reload` `backend/app/config.py:82-85` | [LOCAL]/[POOL] — meaningless for static API endpoints |
| 14 | Telemetry/route evidence: `_istara_route` attached to every chat response — fields `node_id,node_source,provider_type,route_kind,project_id,model,outcome,selected/served/failed_request_count` `backend/app/core/compute_route_evidence.py:14-52`; async span `donor.selected/served/failed` (+`donor.registered/visible/reachable/ready` lifecycle `compute_registry_lifecycle.py:46-79,161-163`) with fields `trace_id,operation,project_id,status,model_name,route_id=donorId:routeKind:counter,donor_id,error_type,error_message,source,event_kind="research_validity"` `compute_route_evidence.py:55-107` → `backend/app/core/telemetry.py:94-147` → span columns `:25-92`; **skipped when project_id empty or "*"** `compute_route_evidence.py:67-69`; node counters set in `_record_selected/_record_success/_record_failure` `compute_registry_routing.py:635-718`; drain hook `compute_route_evidence.py:110-118` | [API] (donor.* naming is pool-flavored but fires for any node) |
| 15 | thinking_mode plumbing: modes `server_default/off/auto/on`, directive injected once into system prompt (never provider JSON fields) `backend/app/core/llm_thinking.py:18-83`; applied registry-side `compute_registry_invocation.py:226,567` and node-side `compute_node_invocation.py:55,176`; **`thinking_mode=None` deliberately passed on inner node.chat to avoid double-injection** `compute_registry_invocation.py:296,635`; output scrubbing always-on: `strip_thinking_blocks` `backend/app/core/llm_output.py:76`, `visible_assistant_content`/`_message` `:143,153`, streaming `ThinkingContentFilter` `:163` (used in every stream loop, e.g. `compute_registry_invocation.py:666,718`); API entry normalization `backend/app/api/routes/chat.py:685-710,895` | [API] |
| 16 | Message sanitation: tool/tool_calls passthrough, role whitelist, empty-content drop, consecutive-system merge, dict/list content preserved `compute_registry_routing.py:747-795` | [API] |
| 17 | Node registration/dedup: endpoint identity canonicalization (scheme,host,port,path; `/v1` stripped; local aliases→"local") `compute_registry_helpers.py:64-145`; duplicate merge with source rank local(30)>relay(20)>network(10) `compute_registry_lifecycle.py:115-169,171-205`; DHCP local-alias detection via RAM/CPU/model-set evidence `:241-297`; capability-preserving merge `:299-328`; `LLMServerEntry` back-compat conversion `:330-350`; heartbeat updates `:395-429` | [API] (identity/merge) / [POOL] (heartbeat, relay-distinct rule `:193-205`) |
| 18 | Project-scope authorization (content boundary): donated relay/browser nodes only receive prompt/embed content when request has concrete project_id inside the node's authenticated donation scope (`allowed_project_ids`, `"*"`) `compute_registry_routing.py:206-228`; enforced again at dispatch `compute_node_invocation.py:20-35`; donor-native-model preference when no project donor supports requested model `compute_registry_invocation.py:184-203,231-238` | [POOL] |
| 19 | Embeddings routing (registry): candidate loop, anthropic skipped, `/api/embed` vs `v1/embeddings`, failures recorded as auxiliary only `compute_registry_invocation.py:924-985` (embed), `:987-1049` (embed_batch) | [API] |
| 20 | Model aggregation/warnings: `list_models` merges per-node lists + capability enrichment (`supports_*`, context, quantization, `is_loaded`, `endpoint_family`, `_server`, `_server_id`) `compute_registry_invocation.py:1051-1147`; capability warnings (no tools / ctx<4096 / ≤2B params) `:1275-1320` | [API] |

# 2. Petals/donated-ONLY code map (migration must NOT touch)

Note: the literal string "Petals" appears only in `backend/app/core/pi_runtime/endpoints.py:4` (docstring naming the donated scheduler); there is no bigscience/Petals library. "Donated pool" = relay websocket + browser donors.

| Surface | File / symbol | Lines |
|---|---|---|
| Relay WebSocket endpoint + compute routes | `backend/app/api/routes/compute.py` — `/compute/nodes`, `/compute/stats`, `/compute/model-warnings`, `/ws/relay` (register/heartbeat/llm_response/embed_response/load_model_response), connection-string scope resolution | whole file; ws protocol `:188-386`; scoping `:54-147` |
| Donor request transport | `backend/app/core/compute_node_transport.py` — `_request_over_websocket`, `fail_pending_requests` | `:298-333` |
| Donor dispatch guards + ws chat/embed branches | `backend/app/core/compute_node_invocation.py` — `_authorized_project_for_content_dispatch` `:20-35`; ws chat `:57-77`; ws stream `:178-191`; ws embed `:323-333`; ws embed_batch `:362-372` |
| Donor scheduling/authorization in registry | `compute_registry_routing.py` — `_node_authorized_for_project_content` `:206-228`; relay/browser candidate guards `:106-109,120-124`; donor-first strict sort `:181-190`; relay ws load_model branch `compute_node_models.py:309-332` |
| Donor-native model preference | `compute_registry_invocation.py:184-203` (`_project_donor_should_choose_native_model`), call sites `:231-238,572-579` |
| Relay/browser chat/stream/embed branches in registry | `compute_registry_invocation.py:288-311` (chat), `:628-648` (stream), `:940-943,1003-1008` (embed), `:1056-1062` (list_models), `total_capacity` `:1192-1200` |
| Donor lifecycle telemetry | `compute_registry_lifecycle.py:38-79` (`_node_project_scopes`, `_emit_lifecycle_once/_snapshot`), `:161-163` (register events), relay heartbeat health `:443-476`; `remove_duplicate_network_nodes` `:366-393` |
| Donor node fields | `backend/app/core/compute_node.py:77-91` (`websocket`, `pending_requests`, `relay_request_timeout_s`, `user_id`, `provider_host`, `allowed_project_ids`, `priority_level`) |
| Donation tokens | `backend/app/core/connection_string.py`, `backend/app/models/connection_string.py`, minting in `backend/app/api/routes/connections.py` (kind `compute_donation`) |
| Relay CLI donor client | `relay/index.mjs`, `relay/lib/{connection,connection-string,heartbeat,llm-proxy,state-machine}.mjs` |
| Browser donor source | `frontend/src/components/common/DonateComputeToggle.tsx` (ws to `/ws/relay` `:124`, registers `user_id:"browser"` → `source="browser"` per `compute.py:301`) |
| Desktop relay launcher | `desktop/src-tauri/src/process.rs:238`, `config.rs:29`, `commands.rs:143,294` |
| Donor test/sim assets | `tests/compute_cases/relay_nodes.py`, `scripts/marathon/relay-simulator.mjs` |
| Pi isolation seam (already Pi-side; do not merge into registry) | `backend/app/core/pi_runtime/endpoints.py` (identity-pinned resolver, explicitly no registry fallback `:61-94`), `backend/app/core/pi_replacement.py:49-60` (`ensure_pi_deepseek_registered` now registers nothing) |

# 3. Local-serving bridge (Ollama / LM Studio)

Base URLs/ports:
- Ollama: `settings.ollama_host = "http://localhost:11434"` `backend/app/config.py:73`; provider inferred from port 11434 `compute_registry_helpers.py:59-60`; discovery probes `11434 /api/tags` `backend/app/core/network_discovery.py:19`.
- LM Studio: `settings.lmstudio_host = "http://localhost:1234"` `backend/app/config.py:78`; inferred from port 1234 `compute_registry_helpers.py:55-56`; discovery probes `1234 /v1/models` `network_discovery.py:18`; generic openai_compat probe `8080 /v1/models` `network_discovery.py:20`.
- OpenAI-compat path building: relative `v1/<suffix>` unless base path already ends `/v1` or `/openai` or host is Gemini `compute_node_transport.py:124-141`; trailing-slash base_url normalization `:105-122`.
- Startup registration: primary+fallback local entries priority 1/5 `backend/app/core/ollama.py:235-287`; configured local node + fallback also in `backend/app/main.py:440-448`; provider autodetect + `.env` persistence `ollama.py:329-370`, default-model resolution/pull `main.py:496-509`.
- LM Studio talks OpenAI-compat for inference (`v1/chat/completions`, `v1/embeddings`, `v1/models`) but **native REST** for management: `api/v1/models` (rich metadata) `model_capabilities.py:446-465`, `api/v1/models/load` `compute_node_models.py:334-361`, `api/v1/models/unload` `:365-407`. Ollama is never addressed via OpenAI-compat for chat: `/api/chat` with `options.num_predict`, NDJSON streaming `compute_registry_invocation.py:315-334,652-681`; `/api/embed` `:948-951`; `/api/tags` `:1066`.

Lost if addressed as plain `openai_compat` endpoints:

| Lost feature | Evidence |
|---|---|
| `ensure_model` / `pull_model` (Ollama `/api/pull`) | `compute_registry_invocation.py:1153-1177`; `ollama.py:74-86,210-223`; admin switch-model pull `api/routes/settings.py:279-288`; embed model bootstrap `embeddings.py:100-103` |
| LM Studio JIT load / auto context reload / unload-on-reload | `compute_node_models.py:282-407`; recovery orchestration `compute_registry_routing.py:350-633`; all branches keyed on `provider_type == "lmstudio"` (`:359,486,571`) — a `openai_compat` label disables them |
| Loaded-state awareness (`is_loaded`, `loaded_context_length`, `trained_context_length`, `loadable`) | LM Studio native metadata `model_capabilities.py:285-331`; Ollama `/api/ps` + `/api/show` `:393-443`; plain `/v1/models` falls back to name heuristics only `:85-172,334-364` |
| Context-window detection depth → global budget sync | rich ctx keys only from native metadata (`:296-315,389-390`); sync path `compute_registry_lifecycle.py:499-504` + `config.py:329-345` would run on 4096-heuristic defaults |
| Ollama structured output via `format` | `llm_schema_adapter.py:102-108,120-121` (openai_compat path would send `response_format`, which Ollama's native API ignores) |
| Ollama options mapping (`num_predict`) + NDJSON stream protocol | `compute_registry_invocation.py:316-334,652-681`; `compute_node_invocation.py:81-105,215-238` |
| Autodetect UX: port-based provider inference, ollama↔openai_compat mislabel auto-correction, network discovery naming, provider autodetect persistence | `compute_registry_helpers.py:38-61`; `compute_node_models.py:49-76`; `network_discovery.py:17-21,113-141`; `ollama.py:329-370` |
| Default embed-model resolution (`nomic-embed-text` / `"embed" in name` preference) | `compute_node_models.py:424-434`; `config.py:74-80` |
| Configured-LM Studio model authority in `_resolve_model` / strict carve-out | `compute_node_models.py:207-273`; `compute_registry_routing.py:249-264` |

# 4. Embeddings

Entry points and signatures:
- `backend/app/core/embeddings.py:42` `async def embed_text(text: str) -> list[float]` — cache-first (`embedding_cache`), then `ollama.embed(text)` (i.e., `compute_registry.embed`, no project_id).
- `backend/app/core/embeddings.py:55` `async def embed_chunks(chunks: list[TextChunk], batch_size: int = 32) -> list[EmbeddedChunk]` — per-chunk cache, batches via `ollama.embed_batch(texts)`.
- `backend/app/core/embeddings.py:100` `async def ensure_embed_model() -> None` — `ollama.ensure_model("nomic-embed-text")`.
- Registry: `compute_registry_invocation.py:924` `embed(text, model=None, project_id=None)`; `:987` `embed_batch(texts, model=None, project_id=None)`. Node-direct: `compute_node_invocation.py:315,354`. Raw client: `ollama.py:179,196`.

Consumers:

| Consumer | file:line | One line |
|---|---|---|
| RAG store | `backend/app/core/rag.py:15` | imports `embed_chunks`/`embed_text` for document indexing + query vectors |
| Prompt RAG | `backend/app/core/prompt_rag.py:313,493` | `embed_text` for prompt-library retrieval |
| Agent memory | `backend/app/core/agent_memory.py:35,59` | `embed_text` for memory search |
| Agent skill tools | `backend/app/core/agent_skill_tools.py:336` | `embed_text` inside skill tool retrieval |
| Agent execution | `backend/app/core/agent_execution.py:822` | `embed_text` during execution-time retrieval |
| Autoresearch RAG params | `backend/app/core/autoresearch_runners/rag_params.py:231` | `embed_text` for autoresearch retrieval |
| Vector health probe | `backend/app/core/vector_health.py:16` | `embed_text` as embedding-path health check |
| File watcher | `backend/app/core/file_watcher.py:13` | `embed_chunks` for re-indexing changed files |
| File processor / agents (types only) | `backend/app/core/file_processor.py:14`, `agent_lifecycle.py:32`, `agent_research.py:32`, `agent_models.py:32` | import `TextChunk` dataclass only |
| Validation consensus | `backend/app/core/validation.py:522` | calls `llm_router.embed_batch(texts, project_id=project_id)` directly — the only project-scoped embed caller |

Can embeddings serve donors? **Mechanically yes, practically almost never.** The registry embed path has a relay/browser websocket branch (`embed_request` over ws: `compute_registry_invocation.py:940-943,1003-1008` → `compute_node_invocation.py:323-333,362-372`), but donated nodes are excluded from candidate selection whenever `project_id` is empty (`compute_registry_routing.py:206-228`). `embed_text`/`embed_chunks` call `ollama.embed(text)` with no `project_id` (`embeddings.py:50,90`), so every consumer above except `validation.py:522` can only ever hit server-owned local/network nodes. Anthropic nodes are excluded from embeddings entirely (`compute_registry_invocation.py:936-937,999-1000`; hard raise `compute_node_invocation.py:337-338,376-377`). Embed failures are recorded as auxiliary (degrade, never offline the chat node) `compute_registry_routing.py:719-730`.